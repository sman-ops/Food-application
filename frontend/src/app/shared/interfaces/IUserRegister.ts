export interface IUserRegister {
  name: string;
  email: string;
  password: string;
  confirmPassword: string;
  address: string;
}
// here we define all the fields that we want to send to the server
